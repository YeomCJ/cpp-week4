#include <iostream>
#include <cstring>
#include <cstdlib>
#include <string>
#include <cstdio>
#include "database.h"

using namespace std;

Database data;
Array ar;

int main() {
    
    int count = 0;
    
    while (true) {
        string s;

        cout << "command (list, add, get, del, exit): ";
        cin >> s;

        if (s == "list") {

        }
        else if (s == "add") {
            
        }
        else if (s == "get") {
            
        }
        else if (s == "del") {
            
        }
        else if (s == "exit") {
            exit(0);
        }
        else {

        }
    }
}